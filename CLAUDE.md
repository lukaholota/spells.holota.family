# char.holota.family — project context

Ukrainian-language D&D character builder and sheet. Live at https://char.holota.family/
Single VPS, single Node process, systemd service `char`, deployed from `main` via GitHub Actions.
~5 000 users, ~200 000 total page views. **This is production with real users. Breaking it is expensive.**

Strategy, current priorities and settled decisions: **[docs/README.md](docs/README.md)**.
Read it before proposing an approach — several obvious-looking alternatives have already been
rejected for reasons recorded in [docs/DECISIONS.md](docs/DECISIONS.md).

## Rules edition — read this first

The platform implements **D&D 5th edition, 2014 rules (PHB 2014 + XGtE + TCoE + supplements).**
It does **NOT** implement the 2024 revision.

When any rules question comes up — how a feature scales, what a feat grants, when a subclass is
picked, how multiclass spell slots work — resolve it against the **2014** sources. Never silently
apply a 2024 rule (species instead of race, origin feats, weapon mastery, subclass at level 3 for
every class, background ASI). If you are not certain which edition a rule belongs to, ask.

2024 support is planned but not started — see [docs/o5-rules-2024/](docs/o5-rules-2024/).

## Stack

- Next.js 16 (App Router, Turbopack) · React 19 · TypeScript
- Prisma 7 (`@prisma/adapter-pg` + `pg` Pool) → PostgreSQL
- next-auth v5 (Google)
- Tailwind 3 + Radix + shadcn-style `src/components/ui`
- zustand (client state), react-hook-form + zod (forms)
- puppeteer-core / pdf-lib for character sheet PDF export
- bun as package manager and runner

## Where things live

| Path | What | Size |
|---|---|---|
| `src/lib/actions/` | Server actions — **all** business logic + DB access lives here | ~8 200 LOC / 18 files |
| `src/lib/logic/` | The only mostly-pure calculation code (bonuses, spell progression, prerequisites) | ~2 300 LOC |
| `src/lib/components/` | Character creator, character sheet, level-up wizard | ~25 500 LOC / 80 files |
| `src/lib/refs/` | Static reference data: dictionary, translation tables, name lists | ~14 000 LOC (mostly data) |
| `src/server/pdf/` | PDF generation | ~2 800 LOC |
| `src/app/` | Routes, thin-ish page shells, some very large clients | ~7 000 LOC |
| `src/domain/pers/` | Abandoned clean-architecture stub. Do not build on it; do not delete without a plan. | ~200 LOC |
| `prisma/schema.prisma` | The whole schema — 56 models + 35 enums, generated by `db:pull` | ~1 830 lines |
| `db/schema.sql` | `pg_dump --schema-only` of prod; rebuilds an empty DB. Generated. | |
| `db/changes/` | Log of SQL actually applied to the DB. Not replayable. | |
| `prisma/seed/` | Content seeds — classes, races, backgrounds, feats, features, items | |

Domain vocabulary in code: **`Pers`** = a player character (Ukrainian «персонаж»). `Character*`
and `Pers*` names both exist and mean the same thing; prefer `Pers` in new code to match the schema.

Production data scale, measured 2026-08-07 — these are real people's characters, not a toy dataset:

| Table | Rows |
|---|---|
| `pers_spell` | 24 942 |
| `pers` | 8 344 |
| `user` / `account` | 803 |
| `spell` | 501 |

## Known state of the codebase — do not be surprised by this

This is honest debt, written down so no session has to rediscover it:

- **No tests.** Zero. No test runner installed. Everything is verified by hand in the browser.
- ~~**No reproducible schema.**~~ Fixed in [KR1.1](docs/o1-safety-net/kr1.1-schema-baseline.md):
  `db/schema.sql` rebuilds the production structure on an empty database, verified against prod.
  The project no longer uses `prisma db push`. This is *not* migrations — see the schema
  workflow below.
- **656 `as any` casts** across `src/`. Types frequently lie, especially around JSON columns
  (`ASI`, choice options, feature effects).
- **Business logic is fused to Prisma.** 28 files import the client directly; `createCharacter`
  and `levelUpCharacter` interleave validation, DB reads, rules math and writes in single
  1 000+ line functions. Nothing is unit-testable as it stands.
- **Very large components.** `CharHomeClient.tsx` (2 263), `LevelUpWizard.tsx` (2 126),
  `spells-client.tsx` (1 510), `MagicSlide.tsx` (1 296), `FeatChoiceOptionsForm.tsx` (1 211).
  Indentation reaches 18 levels in places.
- **Duplicated UI.** The same widget was re-implemented in many places rather than reused; this is
  the direct cause of the "works on 50 screens, breaks on the 51st" bug class.
- **The character creator has up to ~11 dynamic steps**, assembled at runtime in
  `MultiStepForm.tsx` from race/subrace/variant/class/subclass/background/feat choices. The step
  set is combinatorial — this is where most reported bugs come from.
- **No staging environment.** Push to `main` deploys straight to production.

Deleting or "cleaning up" any of this without a test around it is not allowed — see below.

## Working agreements

**Before changing behaviour, pin it.** Any refactor of `src/lib/actions/` or `src/lib/logic/`
needs a characterization test capturing current output first. If a test does not exist yet, write
it as part of the change, not after.

**Reuse before you write.** Before adding a component or helper, search for an existing one
(`rg` by concept, not by filename). A duplicated component is a regression, not a feature.
This codebase's worst failures come from divergent copies of the same widget.

**Translations.** When translating seed data, terms come from `dictionary.json`. UI strings come
from `src/lib/refs/translation.ts`. Never introduce a new constant for a term that already exists
in `translation.ts`.

**Schema workflow — the database is the source of truth.** There are no migrations and none are
wanted. To change the schema:

```
write SQL into db/changes/ → owner applies it to the DB → bun run db:pull → commit
```

`bun run db:pull` = `prisma db pull` + `prisma format` + `prisma generate` + `bun run db:dump`,
so it refreshes `prisma/schema.prisma`, the generated client and `db/schema.sql` in one go. It is
idempotent — running it twice produces byte-identical files.

`prisma/schema.prisma` is a **generated artefact** — never hand-edit it. The one exception is
`///` documentation comments: introspection preserves them, plain `//` comments it deletes. So
anything worth keeping in the schema must be written as `///`. Applied SQL is filed in
`db/changes/` as a log of what was done, not as a sequence to be replayed. No `migrate dev`, no
up/down, no rollback. See [docs/DECISIONS.md](docs/DECISIONS.md) Р2.

Never run `prisma db push`. Never write SQL directly against production yourself — hand the script
to the owner.

**Cloning the database.** `./scripts/db-clone.sh <target> [content|full|schema]` copies the
production DB into another database on the same server. Targets must end in
`_test`/`_staging`/`_dev`/`_scratch`; the script refuses anything else.

**Code style.** Follow the global style rules (minimal comments, verb-named functions, coordinator
function on top reading as named steps, details in small helpers below). Applied here that means:
new server-side work goes into a pure function in `src/rules/` (once it exists) or `src/lib/logic/`,
and the server action only loads, calls, and persists.

**Never** widen a type with `as any` to make something compile. That is how the current 656 got here.

## Commands

```bash
bun i
bunx prisma generate
bun dev            # next dev --turbopack
bun run build
bun run lint
bun run db:pull    # after the owner applied SQL to the DB — regenerates schema + client + db/schema.sql
bun run db:dump    # only db/schema.sql
```

`db:pull` and `db:dump` need Postgres client tools at least as new as the server (16).
`scripts/lib/pg.sh` finds a suitable `pg_dump` even when it is not on `PATH` (e.g. Homebrew keeps
`postgresql@16` unlinked) and fails loudly if none is installed.

`generate:spells` runs automatically on `prebuild`.

## Related repos on this machine

- `../char2024` — a stale full copy of this repo (frozen 2026-01-10) from an abandoned fork attempt
  at 2024-rules support. Its only remaining value is the 2024 data under `dnd-data/` and
  `data/references/`. **Do not merge from it and do not treat it as a second codebase.**
- `../office_apps` — the owner's work project (SvelteKit + telefunc + Prisma-as-codegen +
  prisma-kysely + Kysely). Referenced as the layering pattern this project is moving toward:
  a real `src/server/db/` boundary with no ORM calls inside route handlers.
