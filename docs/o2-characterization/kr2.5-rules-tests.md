# KR2.5 — Тести на правила PHB 2014

**Ціль:** [O2](README.md) · **Статус:** ✅ завершено 2026-08-14 · **Залежить від:** KR2.2

## Навіщо

Golden-файли фіксують, що система робить. Вони **не знають**, чи це правильно. Цей KR — про
правильність.

Кожен тест тут виведений з тексту правил 2014 і має посилання на джерело. Коли він червоний —
це реальний баг, а не зміна поведінки.

## ⚠️ Редакція

**PHB 2014 + XGtE + TCoE.** Не 2024. Якщо джерело правила неоднозначне або схоже на 2024 —
не гадати, а питати власника. Найчастіші пастки:

| Правило            | 2014 (наше)                                          | 2024 (не наше)                     |
| ------------------ | ---------------------------------------------------- | ---------------------------------- |
| Раса/вид           | раса дає ASI                                         | вид не дає ASI, ASI від походження |
| Підклас            | рівень залежить від класу (1/2/3)                    | завжди 3                           |
| Риси               | тільки замість ASI                                   | ще й origin feat від походження    |
| Майстерність зброї | немає                                                | є                                  |
| Походження         | навички + інструменти + риса походження (лише деякі) | завжди ASI + риса                  |

## Готово, коли

- [x] бонус майстерності за рівнем (PHB 15)
- [x] модифікатори характеристик, `floor((score - 10) / 2)`
- [x] HP на 1 рівні й на левелапі (середнє з округленням вгору + CON)
- [x] таблиця слотів заклинань для повного кастера 1-20 (PHB 113 тощо)
- [x] мультикласові слоти (PHB 164) — половинні й третинні кастери округлюються **вниз**
      на рівні класу, потім сумуються
- [x] пактова магія окремо від стандартних слотів (PHB 107)
- [x] AC: без броні 10+DEX, легка +DEX, середня +min(DEX,2), важка без DEX (PHB 144-145)
- [x] кількість підготовлених заклинань по класах
- [x] рівень взяття підкласу по кожному з 12 класів
- [x] рівні ASI по класах (Fighter і Rogue мають додаткові)
- [x] кожна розбіжність із golden записана в [KNOWN-BUGS.md](../KNOWN-BUGS.md)

## Формат

```ts
// PHB 2014, с. 164 «Multiclassing → Spell Slots»
it("половинний кастер додає level/2 округлене вниз", () => {
  expect(findCasterLevel([{ class: "PALADIN", level: 5 }])).toBe(2);
});
```

Посилання на сторінку — обов'язкове. Без нього наступна сесія не зможе перевірити, звідки взялось
очікуване значення, і почне «виправляти» правильний тест.

## Що робити зі знайденим багом

**Не виправляти зараз.** Записати в [KNOWN-BUGS.md](../KNOWN-BUGS.md), позначити тест як
`it.fails(...)` або `it.todo(...)` з посиланням на запис, у відповідному golden-файлі поруч зі
значенням поставити `KNOWN-BUG:`.

Виправлення — після O3. Причина: зараз баг сидить усередині функції на 1400 рядків, і виправляти
його там означає ризикнути всім іншим. Після O3 те саме виправлення буде рядком у чистій функції
з тестом навколо.

## Журнал

**2026-08-13, старт і розвідка BUG-001…011.** Реєстр розділено за природою проблеми: BUG-001/002/003 — прийнята власником довіра сервера до UI; BUG-004/005 — мертві або не застосовні для character-sheet дані; BUG-006/007/010/011 — фактичні розрахункові розбіжності; BUG-008/009 — відкриті рішення про server-side validation. Найвищий практичний вплив у вже підтверджених розрахункових багів має BUG-010: кожен некастер і багато multiclass-персонажів після long rest отримують стандартні слоти за загальним рівнем.

Мінімальні наступні rules-перевірки: BUG-001/002/003 не потребують нових tests, бо це вже прийнята policy «UI — джерело істини» і golden-и фіксують дозволені сервером дані; BUG-004/005 — content-data debt, не rules calculation для character sheet; BUG-006 — Wizard → Fighter, assertion на PHB-164 скорочений набір armor/weapon proficiencies; BUG-007 — Artificer на рівнях 2/6/10/14/18, assertion на ріст `persInfusions` за таблицею TCoE; BUG-008 — Fighter STR 15 / CHA 8 → Paladin має бути rejected за PHB 163-164, але test лишається `it.todo` до рішення власника; BUG-009 — paired creation/multiclass Ranger 1 з порожніми двома обов'язковими виборами, також `it.todo` до рішення, яку з двох поведінок вважати правилом сервера; BUG-011 — Sorcerer 3 після Quickened Spell/long rest має пул Sorcery Points 3 за PHB 101.

Додано окремий від golden `tests/rules/spell-slots.test.ts`: green assertions для full-caster і правила округлення multiclass caster level, а також `it.fails` для BUG-010. Останній стверджує PHB 2014, с. 164-165: Fighter 2 не має стандартних слотів після long rest. Поточний `longRest` передає загальний `pers.level` у таблицю слотів, тож assertion має падати; golden `rest-and-slots.json` лишається без змін і фіксує фактичні `[3, 0, …]`.

Перевірка: `bunx vitest run tests/rules/spell-slots.test.ts --reporter=verbose` — 2 passed, 1 expected fail (3), 3.06 с; `bun run lint` — 0 errors, старі 368 warnings; `bunx tsc --noEmit` — лише відома стара помилка `tests/fixtures/builds/custom-asi-system.ts:20` (string замість number), нових немає. Production-код і golden-и не змінювались.

**2026-08-13, BUG-006.** Додано окремий `it.fails` Wizard → Fighter: за PHB 2014, с. 164 він має отримати середні обладунки, щит і бойову зброю (також легкі обладунки й просту зброю, уже наявні в стартового Wizard). Він не змішується з golden decisive test: golden як і раніше фіксує нульовий приріст, rules assertion очікує правильні категорії.

Перевірка rules suite після цього доповнення: 2 passed, 2 expected fail (BUG-010 і BUG-006), 6.73 с.

**2026-08-13, BUG-007.** Додано окремий `it.fails` Artificer 1→6 за таблицею TCoE, с. 9: чотири infusions на 2 рівні та п'ята на 6. Тест передає на 6 рівні лише новий п'ятий вибір і очікує загалом 5 `PersInfusion`; чинний server action повертає успіх, але ігнорує цей вибір поза рівнем 2.

Перевірка rules suite: 2 passed, 3 expected fail (BUG-010, BUG-006, BUG-007), 11.44 с.

**2026-08-13, BUG-011.** Додано окремий `it.fails` для Sorcerer 3: після ініціалізації
`SORCERY_POINTS` через Quickened Spell long rest має відновити 3 points за PHB 2014, с. 101.
Чинний код відновлює 1, бо глобальний `Feature.findFirst` обирає чужий provider (Storm Rune), а
не Font of Magic персонажа. Це не дублює golden: rules-тест очікує правильне значення, golden
фіксує поточне. Ізольований прогін із тимчасовим очікуванням `1` впав з `Expect test to fail`;
після відновлення `3` тест знову є expected fail. Фінальний rules-suite: 2 passed, 4 expected
fail (BUG-010/006/007/011), 14.26 с; `bunx eslint tests/rules/spell-slots.test.ts` — зелений.

**2026-08-13, базова математика.** Додано чистий rules-suite
`tests/rules/character-advancement.test.ts`: усі 20 рівнів таблиці бонусу майстерності (PHB 2014,
с. 15) і граничні значення формули модифікатора характеристики `floor((score - 10) / 2)` (PHB
2014, с. 13). Це окремо від давнього `bonus-calculator.test.ts`: тут assertions є прямими
посиланнями на правила, а не тестами похідного стану. Тимчасове очікування `+4` на рівні 5 дало
контрольований red `expected 3 to be 4`; правильне `+3` відновлено.

**2026-08-13, HP.** Додано `tests/rules/hit-points.test.ts`: Fighter із CON 14 має 12 HP на
1 рівні (`d10 + 2`) і 20 після 2 рівня (`+ceil(d10/2) + 2`), за PHB 2014, с. 15. Для DB-кейсу
level-up задано timeout 30 с: стандартні 5 с іноді замалі через SSH-тунель. Тимчасове очікування
19 дало контрольований red із фактичним `{ maxHp: 20, currentHp: 20 }`; значення 20 відновлено.

**2026-08-13, full-caster slots.** Додано чистий `tests/rules/full-caster-slots.test.ts`, що
звіряє всі 20 рядків `SPELL_SLOT_PROGRESSION.FULL` із таблицею Wizard (PHB 2014, с. 113). Це
спільне джерело creation, level-up, відновлення слотів і PDF. Тимчасово занижений сьомий слот на
20 рівні (`2 → 1`) дав контрольований red diff; значення 2 відновлено.

**2026-08-13, multiclass і Pact Magic.** Наявний `spell-slots.test.ts` вже напряму перевіряє
формулу PHB 2014, с. 164: половинні й третинні caster levels округлюються вниз на рівні класу до
сумування. Додано чистий `pact-magic.test.ts`: Wizard 5 / Warlock 5 має окремо `casterLevel: 5` і
`pactLevel: 5`, а рядок Warlock 5 дає два слоти 3 кола (PHB 2014, с. 107). Тимчасове очікування
`casterLevel: 10` дало контрольований red; `5` відновлено.

Підсумкова перевірка після цього доповнення: `bunx vitest run tests/rules --reporter=verbose` —
5 файлів, 36 passed, 4 expected fail (BUG-010/006/007/011), 19.28 с; ESLint усіх змінених
rules-тестів — зелений. Production-код і golden-и не змінювались.

**2026-08-14, завершення KR2.5.** Додано `tests/rules/armor-class.test.ts`: чиста фікстура
перевіряє формули AC PHB 2014, с. 144-145 для без броні (`10 + DEX`), легкої (`+ DEX`),
середньої (`+ min(DEX, 2)`) і важкої (`без DEX`) броні. Контрольований red: тимчасове очікування
17 для Chain Mail дало фактичні 16; 16 відновлено.

Додано `tests/rules/prepared-spells.test.ts`: Cleric, Druid, Paladin і Wizard звірені з PHB 2014
(с. 58, 66, 84, 114), Artificer — з TCoE, с. 10. Кейс Wizard 1 / INT 18 очікує 5; тимчасове
очікування 6 дало контрольований red. Додано `tests/rules/class-progression.test.ts`, що через
`spells_test` звіряє `subclassLevel` та `abilityScoreUpLevels` усіх 12 PHB-класів. Artificer
свідомо не входить до цих дванадцяти, бо це клас TCoE; він уже покритий окремим rules-тестом
інфузій. Контрольовані red: Druid 3 замість 2 та Rogue ASI 11 замість 10.

Нових розбіжностей із PHB/TCoE або golden-ами не знайдено, тому `KNOWN-BUGS.md` не змінювався.
Підсумковий прогін після відновлення всіх очікувань: `bunx vitest run tests/rules --reporter=verbose`
— 8 файлів, 69 passed, 4 expected fail (BUG-006/007/010/011), 21.53 с. Як і раніше, Artificer
`it.fails` іноді друкує фоновий P2003/P2025 snapshot після очікуваного падіння, але suite зелений;
це не змінювали. KR2.5 закрито без змін production-коду, golden-ів чи Prisma schema.
