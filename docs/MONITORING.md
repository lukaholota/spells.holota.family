# Моніторинг і аналітика

**Статус:** ✅ Sentry живий на проді · ✅ uptime живий і перевірений на червоному ·
✅ PostHog живий на проді, події з прода перевірені · **Обмеження власника:** платити не будемо,
взагалі — і в жодному з трьох немає платіжного методу

Проєкт має ~5 000 користувачів і **нуль збору помилок**. 61 `console.error` іде в логи
контейнера, яких ніхто не читає; смоук ганяється тільки в момент деплою. Тобто про поломку
дізнаємось так, як записано в [README](README.md): «з треду на Reddit».

## Що беремо і чого не беремо

| Інструмент | Навіщо | Рішення |
|---|---|---|
| **Sentry** | помилки рантайму, серверні й клієнтські | брати першим |
| **Зовнішній uptime** | «сайт лежить» без участі користувачів | брати другим |
| **PostHog** | що люди насправді роблять у конструкторі | брати третім |
| ~~Grafana + Loki~~ | метрики й логи | **ні** — див. [Р11](DECISIONS.md#р11) |

Порядок не випадковий: Sentry відповідає на «що зламалось», uptime — на «чи взагалі живе»,
PostHog — на «чи цим користуються». Перше питання найгостріше.

## Правило, яке робить «не платити» перевіреним, а не обіцяним

**Не прив'язувати платіжку в жодному з трьох сервісів.** Усі троє на безкоштовних тарифах
поводяться однаково: без картки перевищення ліміту призводить до **відкидання подій**, а не до
рахунку. Рахунок технічно неможливий, поки платіжного методу немає.

Додатково, де є така настройка, виставити явний ліміт витрат `$0` — це другий пояс поверх того
самого паска.

Ліміти безкоштовних тарифів **перевірити на місці при реєстрації**, а не за цим документом:
вони змінюються. Записати фактичні цифри сюди.

Для uptime це вже зроблено: приблизна оцінка в цьому абзаці стосувалася UptimeRobot, а взяли
HetrixTools — звірені цифри в розділі [Uptime: що налаштовано](#uptime-що-налаштовано-2026-08-11).

Для PostHog — звірено 2026-08-12, і, на відміну від uptime, приблизна цифра з першої редакції
цього абзацу («~1 млн подій/міс») **підтвердилась**, а не спростувалась: продуктова аналітика
(`Product analytics`) — **1 000 000 подій/міс** у безкоштовному тарифі, і `Billing limit` за
замовчуванням стоїть на тій самій позначці (1 M) — тобто платного шару над безкоштовним просто
немає, піднімати його нема куди без картки. Інші продукти PostHog (Session Replay, Surveys,
Error tracking, Data warehouse тощо) мають власні безкоштовні ліміти, але в цьому проєкті не
використовуються — деталі в [Що зроблено в коді 2026-08-12](#що-зроблено-в-коді-2026-08-12).

## Приватність — вирішити до, а не після

У нас реальні люди, переважно з України, і **сторінки політики приватності на сайті немає**
(перевірено: у `src/app/` немає ні `privacy`, ні банера згоди).

Мінімум, який приймаємо як умову підключення:

- **регіон ЄС** в обох SaaS (`de.sentry.io`, `eu.posthog.com`), а не США;
- у Sentry `sendDefaultPii: false` — не збирати заголовки, куки й IP за замовчуванням;
- у PostHog не писати в події нічого, що ідентифікує людину поза межами вже наявного `userId`;
- питання «чи потрібен банер згоди і сторінка політики» — **вирішене 2026-08-12: ні, для жодного
  з трьох сервісів, див. нижче**.

### Банер згоди: що вирішено, а що ні

Рішення власника 2026-08-11: **Session Replay не беремо.** Саме він робив банер обов'язковим —
запис екрана людини важко подати як щось, на що згода не потрібна.

Що з цього випливає для решти:

| Що збираємо | Банер потрібен? |
|---|---|
| Sentry, `sendDefaultPii: false` | ні — це помилки без IP, кук і заголовків |
| Sentry Session Replay | так — **тому й не беремо** |
| PostHog (крок 3) | **ні — рішення 2026-08-12, деталі нижче** |

Тобто для кроків 1-2 питання закрите: банер не потрібен, бо нема чого ним прикривати.

**PostHog: рішення 2026-08-12 — без банера, без сторінки політики, ціною звуженого збору.**
Юридичний тригер банера згоди (ePrivacy) — це доступ до/запис у **сховище пристрою** (cookie,
localStorage), а не сам факт відправки подій на сервер. Отже банера можна уникнути, не
відмовляючись від PostHog узагалі, а прибравши саме сховище:

- `persistence: "memory"` — жодного cookie чи localStorage; анонімний `distinct_id` не
  переживає перезавантаження сторінки чи нову вкладку;
- `person_profiles: "identified_only"` — профіль особи створюється лише після `identify()`,
  тобто для анонімних відвідувачів PostHog взагалі не веде картку людини;
- `autocapture: false`, `capture_heatmaps: false` — лише навмисно надіслані події
  (`character_created` і подібні), не сирі кліки;
- для залогінених `identify(userId)` у `PostHogProvider` дає повну кореляцію подій одного
  користувача між візитами **без жодного стороннього ідентифікатора** — прив'язка йде до вже
  наявного акаунта, не до нового.

**Що це коштує.** Країна/пристрій **не** втрачаються — вони резолвляться на льоту з IP і
User-Agent, а не з cookie. Втрачається одне: кросвізитне зчеплення **анонімних** (не
залогінених) відвідувачів — не можна сказати «цей самий анонімний браузер заходив і в
понеділок, і в четвер». Для питань цього проєкту (що робить залогінений користувач у
конструкторі) це прийнятна ціна. Autocapture вимкнений — це окреме рішення про сигнал/шум
(власні події зручніші для дашбордів), не про приватність саму по собі.

Сторінка політики приватності лишається на «колись» — банер був єдиною причиною робити її
терміново на цьому кроці, і причини не стало.

## Що вже перевірено в коді

Щоб наступна сесія не відкривала це заново:

| Факт | Значення для роботи |
|---|---|
| `src/app/error.tsx` і `src/app/global-error.tsx` **є** | точки інтеграції Sentry на місці |
| `src/instrumentation.ts` **немає** | створювати з нуля (Next 16 читає його для серверної ініціалізації) |
| `next.config.ts` мінімальний, **CSP немає** | сторонні скрипти нічим не блокуються |
| залежностей моніторингу в `package.json` **нуль** | чистий старт, конфліктів не буде |
| 61 `console.error` у `src/` | після Sentry вони стають придатними до читання, а не сміттям |

## Що зроблено в коді 2026-08-11

`@sentry/nextjs@10.70.0`, помилки + трейси 10%, **без Session Replay і без логів**.

| Файл | Роль |
|---|---|
| `src/instrumentation.ts` | серверна реєстрація + `onRequestError` — ловить усі серверні помилки запиту |
| `src/instrumentation-client.ts` | ініціалізація в браузері + `onRouterTransitionStart` |
| `src/sentry.server.config.ts` · `src/sentry.edge.config.ts` | `Sentry.init` на кожен рантайм |
| `src/lib/monitoring/sentry-options.ts` | спільні опції — `sendDefaultPii: false` і `enabled` в одному місці на всі три рантайми |
| `src/lib/monitoring/report-error.ts` | `reportBoundaryError` для `error.tsx` і `global-error.tsx` |
| `src/lib/monitoring/sentry-check-access.ts` | токен-ворота для перевірочних маршрутів |
| `src/app/sentry-check/` · `src/app/api/sentry-check/` | навмисні помилки: клієнтська і серверна |

Edge-конфіг доданий наперед: `middleware.ts` у проєкті немає, але перший же доданий middleware
інакше опинився б поза моніторингом мовчки.

**Поза продом Sentry вимкнений** — `enabled: process.env.NODE_ENV === "production"`. Помилки з
`bun dev` — це помилки, яких ще не бачив жоден користувач: вони з'їдали б квоту і змішувалися б
у панелі зі справжніми. Вимкнення явне, а не «локально ж немає DSN»: один рядок у `.env` — і
розробка почала б слати події в прод-проєкт, причому мовчки.

**Перевірено локально** (`bun run build`, Next 16.1.6 на Turbopack, код виходу 0):

- `withSentryConfig` із Turbopack збирається без жодного попередження — окремих дій не треба;
- DSN лежить у **клієнтських** чанках `.next/static/`, тобто механізм `NEXT_PUBLIC_*` пройшов;
- SDK і трейсинг реально в бандлі (`SENTRY_ORIGIN`, `sentry-trace`,
  `SENTRY_IDLE_SPAN_FINISH_REASON`), а не тільки згадка змінної;
- серверна половина в `.next/standalone/` — 45 файлів, `captureRequestError` на місці;
- жодного `.map` нашого коду не роздається (єдиний `.map` у `static/` — поліфіл самого Next).

**Не перевірено:** що подія доїжджає в Sentry. Це можливо лише з прода, з живим DSN.

## Головна пастка, на якій цей проєкт уже раз погорів

**`NEXT_PUBLIC_*` Next вшиває в клієнтський бандл під час `next build`.** У рантаймі підставити
їх неможливо, скільки б їх не було в `char.env`.

Це вже коштувало зламаного Google One Tap після переїзду
([журнал](MIGRATION-HETZNER.md#журнал), 2026-08-09): змінна була в оточенні контейнера, а в
надрукованому `.js` — ні.

Отже `NEXT_PUBLIC_SENTRY_DSN` і `NEXT_PUBLIC_POSTHOG_KEY` **обов'язково**:

1. `ARG` + `ENV` у [`Dockerfile`](../Dockerfile), поруч із `NEXT_PUBLIC_GOOGLE_CLIENT_ID`;
2. `build-args` у джобі `image` в [`deploy.yml`](../.github/workflows/deploy.yml);
3. і лише потім — у `char.env` на сервері, для серверної частини.

Запобіжник із One Tap варто повторити: збірка **падає** на порожньому значенні. Мовчазно
зламаний моніторинг гірший за відсутній — він створює враження, що за системою стежать.

## Пастки, знайдені під час підключення Sentry

Усі чотири тихі — жодна не проявляється як помилка збірки.

**1. `dataCollection: {}` вмикає збір даних, а не налаштовує його.** У сніпетах самого Sentry це
поле стоїть із закоментованим вмістом, тобто фактично порожнім об'єктом. Порожній об'єкт
**перемикає всі незадані категорії на дозвільні значення** — IP, куки, заголовки. Скопіювати
їхній приклад як є = увімкнути рівно те, що заборонено. Тому поле не передається взагалі, а
приватність тримається на `sendDefaultPii: false`.

**2. У `@sentry/nextjs` v11 `sendDefaultPii` приберуть, і дефолт стане «збирати».** Це не здогад:
у самому пакеті лежить `TODO(v11): Remove this function along with sendDefaultPii. Once
dataCollection is the only API, the DEFAULTS (including userInfo: true) will always apply.`
Тобто **мажорний апгрейд мовчки увімкне PII.** На ньому треба переписати опції на явний
`dataCollection`, а не просто підняти версію.

**3. Серверна помилка створює дві події, якщо ловити її в межі помилок.** Next віддає в
`error.tsx` уже не саму помилку, а огризок із `digest` і без стека — сама вона тим часом поїхала
в Sentry через `onRequestError`, зі справжнім стеком. `captureException` у межі додав би другу,
беззмістовну подію на кожну серверну поломку і вдвічі швидше з'їдав квоту. Тому
`reportBoundaryError` пропускає все, що має `digest`.

**4. Source maps, увімкнені без вивантаження, гірші за вимкнені.** Вони не «просто не
завантажаться в Sentry» — `.map` ляжуть поруч зі збіркою і роздаватимуться публічно, тобто
вихідний код сайту стане доступним усім. Тому в `next.config.ts` стоїть явний
`sourcemaps: { disable: true }`, а не відсутність налаштування. Вмикати одночасно з токеном.

## Про візард `@sentry/wizard`

Не використовується, і це рішення, а не лінь. Причини за спаданням важливості:

1. Він створює публічні `/sentry-example-page` і `/api/sentry-example-api`, які кидають помилку
   кожному, хто їх відкриє. На сайті з ~5 000 користувачів це відкритий кран у безкоштовну
   квоту: вичерпавши її, Sentry почне відкидати **справжні** помилки.
2. Його дефолти — `sendDefaultPii: true` і Session Replay, тобто рівно те, що тут заборонено.
3. Головного він однаково не робить: про `ARG`/`ENV` у `Dockerfile` і `build-args` у
   `deploy.yml` він не знає, тож вийшло б «у контейнері змінна є, у бандлі немає».

Окремо: **в агентному середовищі він не запускається взагалі** — `--non-interactive` не рятує,
він усе одно смикає TTY і падає з `ERR_TTY_INIT_FAILED`. Запустити його може лише власник у
своєму терміналі.

## Окремо про Sentry і source maps

Стектрейси з мініфікованого бандла нечитабельні. Завантаження мап робиться на етапі збірки й
потребує `SENTRY_AUTH_TOKEN` — тобто ще один секрет у CI **і** ще один BuildKit-секрет, як уже
зроблено для `database_url`. Токен у шарах образу осідати не повинен.

Це не обов'язкове для першого кроку: без мап Sentry працює, просто трейси гірші. Якщо ускладнює —
відкласти й записати як хвіст.

## Uptime: чому саме ззовні

Монітор на тій самій машині не повідомить, що машина лягла. Це єдина вимога, яка не залежить від
ресурсів і не протухає.

**Uptime Kuma** годиться, але їй потрібна **інша** машина — а другої в нас немає (старий VPS
гаситься після 2026-08-17). Тому на першому кроці SaaS.

Що моніторити — не `/`, а **`/api/health`**: сторінкові маршрути віддають 200 навіть із мертвою
базою, це перевірено 2026-08-09 і саме тому роут узагалі з'явився
([KR1.5](o1-safety-net/kr1.5-smoke-before-deploy.md)).

## Uptime: що налаштовано 2026-08-11

**HetrixTools**, безкоштовний тариф, платіжка не прив'язана (реєстрація її не просить). Не
UptimeRobot: у того на безкоштовному інтервал 5 хв і **Telegram не входить** у безкоштовні
інтеграції, а тут входить.

Монітор один, `b56d0cbeb9445af4481e2c3b06347632`. Значення нижче **зчитані назад з API**, а не
переписані з того, що надсилалось:

| Поле | Значення | Навіщо саме так |
|---|---|---|
| `target` | `https://char.holota.family/api/health` | не `/`, див. розділ вище |
| `http_method` | `GET` | **дефолт HEAD, і це пастка** — див. нижче |
| `keyword` | `"database":true` | падіння, якщо тіло не підтверджує базу |
| `accepted_http_codes` | `[200]` | 503 від мертвої бази = падіння |
| `check_frequency` | `1` хв | на безкоштовному тарифі, не 5 |
| `number_of_tries` | `2` | одна невдача поспіль тривоги не підіймає |
| `triggering_locations` | `2` (з 3) | глюк однієї локації тривоги не підіймає |
| `locations` | Франкфурт, Амстердам, Лондон | сервер у Німеччині, користувачі в Україні |
| `verify_ssl_certificate` | `true`, попередження за 10 днів | прострочений сертифікат — теж «сайт лежить» |
| `max_redirects` | `0` | редирект на `/api/health` був би аномалією |

Сповіщення — **Telegram**, через список `Default Contact` (`d003ec1e3ed279c7c7f428f8774cc955`);
у тому ж списку лишається пошта як другий канал. Вибір власника 2026-08-11.

**Фактичні ліміти безкоштовного тарифу**, звірені при реєстрації: 15 моніторів доступності,
інтервал 1 хв, keyword-монітори й Telegram включені, картка не потрібна, **1 000 викликів API на
місяць**. Останнє стосується лише керування через REST — самі перевірки (≈130 тис./міс із трьох
локацій) у нього не рахуються. Ендпоінта, який показував би ліміти тарифу, в API **немає** —
цифри звірені в панелі.

### Як цим керувати далі

Токен API живе **тільки в панелі HetrixTools** — його немає ні в репозиторії, ні в `char.env`, і
він там не потрібен: uptime нічого не вимагає ні від сервера, ні від збірки. Це єдиний із трьох
сервісів, який не має точки дотику з кодом. Щоб змінити монітор, треба взяти новий токен у
https://hetrixtools.com/dashboard/account/api/ — і пам'ятати про поділ версій та про те, що
перестворення під час аварії не спрацює (пастки 4 і 2 нижче).

### Ціна, яку приймаємо

Виявлення не миттєве: два невдалі прогони по хвилині з двох локацій. Виміряно на перевірці —
**19 с** для падіння за кодом відповіді і **80 с** для падіння за тілом. Це краще за очікувані
5 хв, але це не нуль.

### Пастки HetrixTools

**1. `Method` за замовчуванням — `HEAD`.** HEAD не повертає тіла взагалі, тож перевірка на
`"database":true` не мала б що перевіряти. Монітор при цьому виглядав би цілком налаштованим і
зеленим. Тому `Method: "GET"` задається явно, а `http_method` перечитується з API після створення.

**2. Монітор, створений по мертвій цілі, не запускається ніколи.** HetrixTools показує
«Monitoring Not Started» і прямо каже: `No downtime notifications will be sent until monitoring
starts`. Два наслідки:

- **перестворювати монітор під час аварії безглуздо** — він не почне працювати, доки сайт не
  підніметься;
- **перевірка на червоному мусить іти від зеленого до червоного**, а не починатися з червоного.

**3. `POST v2/uptime/edit/` — правка точкова.** Незадані поля зберігаються, а не скидаються в
дефолти (перевірено перечитуванням: після зміни одного `Keyword` метод, локації й список контактів
лишились на місці). Якби скидалися, будь-яка правка тихо повертала б `Method` у `HEAD`.

**4. Версії API розділені.** Читання — `v3` (`GET /v3/uptime-monitors`, заголовок
`Authorization: Bearer`), а створення, правка й видалення — `v2`
(`POST /v2/<TOKEN>/uptime/{add,edit,delete}/`). Одиночних маршрутів `/v3/uptime-monitors/<id>`
немає — на PATCH і PUT відповідає `invalid endpoint`.

**5. Токен не працює, доки не підтверджено пошту.** До підтвердження задокументований ендпоінт
відповідає `Invalid API Token`, тобто помилка виглядає як зіпсований ключ, а не як неактивований
акаунт. Ознака, що шлях правильний, а справа в токені: неправильний шлях дає інший текст —
`Invalid API Call`.

### Чого ця перевірка не доводить

Що падіння **машини цілком** підніме тривогу. Гасити прод заради цього не будемо; шлях у
HetrixTools там той самий, що для перевірених випадків, але записано це як міркування, а не як
перевірка.

Приватності це все не стосується: uptime не збирає про користувачів нічого. Умови про регіон ЄС
і банер згоди — це про Sentry і PostHog, не про цей крок.

## Що зроблено в коді 2026-08-12

`posthog-js@1.415.6`, лише клієнтський — сервернної половини (`posthog-node`) тут немає.
Конфігурація приватності — [Банер згоди: рішення 2026-08-12](#банер-згоди-що-вирішено-а-що-ні)
вище.

| Файл | Роль |
|---|---|
| `src/lib/monitoring/posthog-options.ts` | спільні опції приватності — `persistence: "memory"`, `autocapture: false`, `person_profiles: "identified_only"` в одному місці |
| `src/lib/monitoring/posthog-provider.tsx` | ініціалізація (лише прод) + `identify(userId)` на логіні / `reset()` на виході |
| `src/lib/monitoring/posthog-check-access.ts` | токен-ворота для перевірочного маршруту, той самий патерн, що в Sentry |
| `src/app/posthog-check/` | навмисна подія `posthog_check` для перевірки з прода |
| `src/lib/components/characterCreator/MultiStepForm.tsx` | перша інструментована подія — `character_created` (лише ID класу/раси/підраси/варіанту/передісторії, без імені персонажа й будь-якого вільного тексту) |

`NEXT_PUBLIC_POSTHOG_KEY`/`NEXT_PUBLIC_POSTHOG_HOST` вшиті так само, як Sentry і Google One
Tap: `ARG`+`ENV` у `Dockerfile` з падінням на порожньому значенні, `build-args` у джобі `image`
в `deploy.yml`, значення в GitHub Secrets.

**Регіон ЄС підтверджений не обіцянкою, а рядком** — так само, як з Sentry DSN: клієнтський
чанк на публічному URL містить `eu.i.posthog.com`, знайдено пошуком по чанку, не по
`printenv` у контейнері.

**Перевірено з прода 2026-08-12.** `/posthog-check?token=...` відкрита, кнопка шле
`posthog_check`, подія знайшлась у PostHog Activity разом із фоновими подіями реальних
відвідувачів (`Pageview`, `Web vitals`, `Identify` — сама аналітика вже працює на живому
трафіку, паралельно з перевіркою). Зразкових/демо-подій у новому проєкті PostHog **немає** —
на відміну від Sentry, порожній проєкт показував лише банер «this project has no events yet»,
без готового фейкового event.

**Одна знахідка під час перевірки, варта запису.** Activity в панелі PostHog відстає від
реальної відправки на кілька хвилин (у власника був момент, коли подія вже пішла з браузера —
Network tab це не встиг зафіксувати через швидкість React-рендеру, — а в Activity вона зʼявилась
тільки хвилин через 5). Це затримка **відображення**, не відправки: подія була доставлена
одразу, просто індексація в UI панелі не миттєва. Не плутати з реальним недоставленням.

### Ad-blocker проксі — зважено, не увімкнено

На відміну від Sentry (гіпотезу перевірили й відкинули на кроці 1), PostHog — типова ціль
блоклистів (EasyPrivacy та подібні). Офіційна відповідь PostHog — проксювати `api_host` через
власний домен (`/ingest` → `eu.i.posthog.com` через `rewrites()` у `next.config.ts`), тоді
запити йдуть на перший домен і доменні правила блоклистів їх не бачать.

**Чому не увімкнено зараз.** Це не про складність — зміна невелика (кілька рядків у
`next.config.ts` + заміна `api_host` на відносний шлях). Причина в тому, що доказів проблеми
поки немає: перевірка 2026-08-12 із браузера власника (з розширеннями, включно з
adblock-подібним) доставила і `identify()`, і кілька `posthog_check` без жодної ознаки
блокування. Це один браузер, не доказ для всієї бази з ~5 000 користувачів — але й немає
підстави вмикати проксі про всяк випадок.

**Коли переглядати.** Порівняти лічильник `$pageview` у PostHog з лічильником переглядів у
Umami за той самий період (обидва скрипти стоять на кожній сторінці — `layout.tsx`, Umami
відомий у коді, PostHog новий). Якщо PostHog стабільно й суттєво відстає — це і є сигнал, що
блоклисти ріжуть саме його, і тоді проксі вмикається не про всяк випадок, а з конкретною
причиною.

## Готово, коли

- [x] Sentry ловить справжню помилку з прода — перевірено навмисно кинутою, а не «інтеграція є»
      · 2026-08-11
- [x] у Sentry видно і серверну, і клієнтську помилку (це два різні шляхи) · 2026-08-11,
      причому клієнтських перевірено **дві**: через глобальне перехоплення SDK і через `error.tsx`
- [x] `NEXT_PUBLIC_*` доїжджають **у бандл** — перевірено пошуком по чанку на публічному URL,
      як робили з One Tap, а не по `printenv` у контейнері · 2026-08-11
- [x] uptime б'є в `/api/health` і **перевірений на червоному** · 2026-08-11 — не зупинкою
      контейнера і не підміною маршруту, а тимчасовим монітором, якому після зеленого стану
      підмінили очікування; обидва сповіщення дійшли в Telegram
- [x] PostHog отримує події з прода · 2026-08-12 — `posthog_check`, згенерована власником
      натисканням кнопки на `/posthog-check`, а не «інтеграція є»
- [x] у жодному з трьох сервісів **немає платіжного методу**; де є ліміт витрат — стоїть `$0` ·
      HetrixTools і PostHog підтверджено раніше; **Sentry підтверджено 2026-08-12** — власник
      закрив хвіст, що висів із кроку 1: картку туди теж не додавав
- [ ] фактичні ліміти безкоштовних тарифів записані в цей файл · HetrixTools і PostHog — так
      (PostHog 2026-08-12, розділ [Правило...](#правило-яке-робить-не-платити-перевіреним-а-не-обіцяним)).
      **Sentry — ні**: платіжки нема (підтверджено), але реальну цифру квоти помилок/міс так і не
      звірили в панелі — у коді лишився ненадійний приблизний «~5 тис. помилок/міс»
- [x] рішення щодо банера згоди прийняте й записане — **для Sentry**: Session Replay не беремо,
      тому банер не потрібен. **Для PostHog**, 2026-08-12: `persistence: memory` прибирає сам
      тригер (сховище пристрою), банер не потрібен — деталі в [Банер згоди](#банер-згоди-що-вирішено-а-що-ні)
- [x] сформульоване продуктове питання, на яке ці дані відповідають · «скільки персонажів
      реально створюється і яких класів/рас/передісторій люди обирають» — саме на це відповідає
      подія `character_created` з наявними властивостями вже зараз, без додаткової
      інструментації. Питання про лійку «почав → зберіг» лишається на майбутнє — воно потребує
      ще й події на вхід у конструктор, якої зараз немає

## Журнал

### 2026-08-11 — Sentry: код готовий, події ще не їздять

**Що зроблено.** `@sentry/nextjs@10.70.0` підключений руками: помилки + трейси 10%, регіон ЄС,
`sendDefaultPii: false`, без Session Replay. Перелік файлів і локальні перевірки — у розділі
[Що зроблено в коді](#що-зроблено-в-коді-2026-08-11), знайдені пастки —
у [Пастки](#пастки-знайдені-під-час-підключення-sentry).

**Чого ще немає.** Жодна подія в Sentry не бачена. Це не «майже готово»: без DSN у секретах
збірка навіть не пройде — `Dockerfile` навмисно падає на порожньому `NEXT_PUBLIC_SENTRY_DSN`, з
тієї самої причини, з якої він падає на порожньому `NEXT_PUBLIC_GOOGLE_CLIENT_ID`. Отже секрет
має з'явитися **до** пуша, інакше перший же деплой стане червоним.

**Session Replay — ні.** Рішення власника. Він єдиний із трьох сигналів робив банер згоди
обов'язковим, і саме тому питання банера для кроків 1-2 знято повністю, а не «вирішимо потім».

**Регіон ЄС підтверджений** — у виданому DSN хост `o…ingest.de.sentry.io`. Умова приватності з
[Р11](DECISIONS.md#р11) виконана не обіцянкою, а перевіркою рядка.

**Обережно зі «зразковою» помилкою в панелі.** Sentry створює в кожному новому проєкті готовий
іш'ю `TypeError: Object [object Object] has no method 'updateFrom'` зі стеком у
`sentry/scripts/views.js` і юзером `mail@example.org`. Він виглядає як справжня подія і легко
читається як «все працює», хоча застосунок ще жодного разу нічого не слав. Ознака — банер
«You are viewing a sample error» угорі сторінки. Перевіркою вважається лише подія з
`/sentry-check`, з нашим текстом.

**Трейси 10%, а не 0.** Квота на спани окрема від квоти на помилки (10 млн проти ~5 тис.), при
поточному трафіку це порядок 60 тис. спанів/міс — не близько до межі. Без них у події видно, що
впало, але гірше видно де.

**Візард не використовується** — окремий розділ вище пояснює чому. Спроба прогнати його в
одноразовій копії репозиторію (щоб просто подивитись на вивід) впала з `ERR_TTY_INIT_FAILED`
навіть із `--non-interactive`.

**Дрібниця, яка вилізе на source maps:** bun блокує постінстал `@sentry/cli` (він качає бінарник).
Зараз це ні на що не впливає, бо вивантаження мап вимкнене, але коли дійде до нього — це перше
місце, куди дивитись.

**Граблі, на які наступив я сам:** читав код виходу через `${PIPESTATUS[0]}` у zsh, де ця змінна
порожня, і `set -u` обірвав скрипт посеред роботи. Це вже записано в доках проєкту — і все одно
спрацювало. Код виходу читати напряму, без конвеєра.

### 2026-08-11, за кілька годин — викочено, події доїжджають

Деплой зелений (`checks` → `image` → `deploy`, смоук пройшов, відкату не було). Перевірено на
живому сайті, по одній події на кожен шлях:

| Шлях | Іш'ю в Sentry |
|---|---|
| серверний, `onRequestError` | `навмисна серверна помилка`, `Unhandled`, `GET /api/sentry-check` |
| клієнтський, глобальне перехоплення SDK | `навмисна клієнтська помилка в обробнику` |
| клієнтський, через `error.tsx` | `навмисна клієнтська помилка рендера` |

DSN підтверджений не в контейнері, а **в чанку з публічного URL** —
`/_next/static/chunks/e4f0cf1d42497fac.js` містить і ключ, і `ingest.de.sentry.io`.

**Захист від дублів працює.** У серверного іш'ю `Events = 1`, а не 2: `error.tsx` відпрацював,
але `reportBoundaryError` пропустив подію з `digest`, бо її вже надіслав `onRequestError`. Це
рівно те, заради чого писався той запобіжник, і тепер воно перевірене, а не обіцяне.

**Блокувальники реклами тут ні до чого — поки що.** Була гіпотеза, що клієнтські події ріже
uBlock (типова причина «серверні є, клієнтських нема»). Не підтвердилась: події не з'являлися
просто тому, що кнопки ще не натиснули. Опція `tunnelRoute`, яка лікує саме цей клас проблем,
лишається невикористаною — і це нормально, доки немає ознак, що когось ріже.

**Дрібна нерівність, яку варто знати.** `/sentry-check` без токена віддає **200** із намальованою
404-сторінкою, а не статус 404: відповідь уже почала стрімитись, коли спрацював `notFound()`.
Ворота при цьому працюють — вміст сторінки не рендериться, кнопок немає. `/api/sentry-check` без
токена віддає чесний 404. Виправляти зараз не варто, але якщо колись здасться, що ворота зламані
— справа в статусі, не в доступі.

**Лишилось для закриття кроку 1:** підтвердити, що в Sentry немає платіжного методу, виставити
ліміт `$0` і записати сюди фактичні ліміти безкоштовного тарифу замість приблизних.

### 2026-08-11 — Крок 2: зовнішній uptime працює і перевірений на червоному

**Що зроблено.** Один монітор HetrixTools на `/api/health`, сповіщення в Telegram. Налаштування,
звірені цифри тарифу й п'ять знайдених пасток — у розділі
[Uptime: що налаштовано](#uptime-що-налаштовано-2026-08-11).

**Перевірка на червоному вийшла не такою, як планувалась, і це найцінніше з усього кроку.**
Задум був простий: тимчасовий монітор на завідомо мертвий шлях (`/api/health-nope`, який справді
віддає 404), нічого не ламаючи в проді. Він **не спрацював би** — і мовчки. HetrixTools не
запускає монітор, доки хоч раз не побачив ціль здоровою: обидва тимчасові зависли в стані
`Monitoring Not Started`, а поруч написано `No downtime notifications will be sent`. Тобто я б
сидів і чекав сповіщення, якого за задумом не існує, і міг би прийняти тишу за «ще не час».

Спіймано це не міркуванням, а тим, що власник подивився в панель і спитав, чи воно точно
надішлеться. Мораль ширша за цей крок: **«монітор створений» і «монітор працює» — різні стани**,
і в цьому сервісі вони ще й виглядають однаково зеленим словом `Active`.

**Як перевірено натомість — від зеленого до червоного.** Обидва тимчасові монітори спершу
наведені на здорові цілі, доведені до `up` (тобто моніторинг реально стартував), і аж тоді
зламані підміною **очікувань монітора**, а не самої цілі. Прод не чіпався взагалі:

| Тимчасовий монітор | Як зламано | Що доводить |
|---|---|---|
| по тілу | очікуване слово змінено на `"database":false` | сайт віддає 200, а монітор червоніє — саме той сценарій «200 із мертвою базою», заради якого перевіряється тіло |
| по коду | ціль переведена з `httpbin.org/status/200` на `/status/503` | живий 503 вважається падінням, а не «не 200, ну й гаразд» |

О 10:24:16 зламано обидва, о 10:24:27 прод окремо перевірений живим (200,
`{"status":"ok","database":true}`) — щоб причина падіння першого монітора не допускала другого
тлумачення. Зміна статусу: **10:24:35** (за кодом) і **10:25:36** (за тілом). Обидва сповіщення
**дійшли в Telegram**, підтверджено власником. Тимчасові монітори видалені, лишився один.

**Що це закриває.** Правило проєкту «тест, який жодного разу не був червоним, не рахується»
виконане не обіцянкою: чекбокс поставлений після того, як сповіщення реально прийшло.
Незакритий хвіст із [KR1.5](o1-safety-net/kr1.5-smoke-before-deploy.md) — гілка автовідкату, яка
ніколи не виконувалась — лишається на місці, це інша перевірка.

**Дрібниці, які коштували часу:**

- **токен не працює, доки не підтверджено пошту**, і помилка при цьому — `Invalid API Token`,
  тобто виглядає як зіпсований ключ. Відрізнити можна за тим, що неправильний шлях дає інший
  текст (`Invalid API Call`);
- **`httpstat.us` мертвий** (таймаут), для перевірки кодів відповіді придатний `httpbin.org`;
- на сервері **немає ні `limit_req` у nginx, ні fail2ban, ні ufw** — перевірено перед
  налаштуванням, бо монітор з інтервалом 1 хв із трьох локацій інакше міг би впертися в захист і
  давати фальшиві падіння.

### 2026-08-12 — Крок 3: PostHog живий, без банера, без сторінки політики

**Що зроблено.** `posthog-js` підключений клієнтом-only: `persistence: "memory"`,
`autocapture: false`, `person_profiles: "identified_only"`, без Session Replay. Файли й локальні
перевірки — [Що зроблено в коді 2026-08-12](#що-зроблено-в-коді-2026-08-12). Перша подія —
`character_created`.

**Знахідка перед початком роботи, ніде раніше не записана.** У `src/app/layout.tsx` вже стоїть
`<Script src="https://cloud.umami.is/script.js">` — Umami, без кук, без банера. Ніхто це не
задокументував — ні в цьому файлі, ні в [Р11](DECISIONS.md#р11). Корисно було двічі: по-перше,
живий приклад «аналітика без банера вже працює в цьому проєкті» задав орієнтир для рішення про
PostHog; по-друге, лічильник Umami тепер придатний як контрольна точка для питання про
блокувальники реклами нижче.

**Рішення про банер — не «взяти чи ні», а «як налаштувати».** Власник спершу питав, чи варіант
без кук («варіант 1») означає відмову від країни й пристрою. Ні — обидва резолвляться на льоту
(геоIP і User-Agent), не з cookie. Реальна ціна варіанта 1 — лише кросвізитне зчеплення
анонімних відвідувачів, і для залогінених це не проблема (`identify(userId)` без cookie дає те
саме). Деталі — [Банер згоди](#банер-згоди-що-вирішено-а-що-ні).

**Онбординг PostHog:** три перемикачі за замовчуванням увімкнені (Autocapture, Heatmaps, Web
vitals). Вимкнено перші два — суперечать рішенню про власні події. Web vitals лишено —
продуктивність, не поведінка, не стосується банера.

**Перевірка з прода пройшла не з першого погляду.** Власник відкрив `/posthog-check`, натиснув
кнопку, у Network tab — жодного запиту. Гіпотеза була — блокувальник реклами (в тулбарі браузера
видно ABP-подібне розширення, а PostHog, на відміну від Sentry, класична ціль блоклистів).
Виявилось хибною тривогою: подія просто ще не встигла піти в момент погляду в Network tab, а
Activity в панелі PostHog відстає від фактичної відправки на кілька хвилин — за 5 хвилин
з'явилось не одна, а кілька копій `posthog_check` (власник тиснув кнопку кілька разів, поки
чекав). Зразкових/демо-подій у новому проєкті PostHog не знайдено — на відміну від Sentry,
порожній проєкт не підсовує готового фейкового event.

**Ad-blocker проксі — зважено, не увімкнено.** Причина і умова перегляду —
[окремий розділ вище](#ad-blocker-проксі--зважено-не-увімкнено). Коротко: доказів проблеми
поки немає, зміна невелика, критерій вмикання — порівняння з лічильником Umami.

**Пастка з деплоєм, варта запису на майбутнє.** `POSTHOG_CHECK_TOKEN` — серверна змінна, живе
лише в `char.env`, у збірку не вшивається. Але `docker start` не підхоплює новий
`--env-file`, тож контейнер довелось перестворити. Замість руками зібраного `docker run`
використали наявний `deploy-container.sh release <той самий SHA-образ>` — він підняв контейнер
на вільному слоті з оновленим `char.env`, перевірив `/api/health` і перемкнув upstream без
простою, після чого `retire` прибрав старий. Тобто скрипт викочування придатний і для «змінити
тільки env, образ той самий», не тільки для нового реліза. Сам запис у `char.env` і виклик
`deploy-container.sh` на сервері агент виконати не зміг — пісочниця сесії блокує запис у
продові конфіги через SSH, тому цю частину зробив власник за готовими командами.

**Незакритий хвіст, не з цього кроку — частково закритий тут же.** Перевірка білінгу для Sentry
(крок 1) висіла з журналу 2026-08-11 («лишилось для закриття кроку 1»). Власник підтвердив того
ж дня, 2026-08-12: картку в Sentry теж не додавав. Платіжної частини питання більше нема ні в
одному з трьох сервісів. Лишається вужчий хвіст — саму цифру квоти (помилок/міс) для Sentry в
панелі так і не звіряли, у коді досі приблизне «~5 тис.».
